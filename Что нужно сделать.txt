Что нужно сделать:

Считать данные из ozon_orders.json с помощью Python.

Создать структуру базы данных (используй PostgreSQL или SQLite).

Загрузить данные в таблицу, при этом:

корректно преобразовать дату заказа (date);

извлечь вложенные поля (например, customer.region);

сохранить заказы с полями: order_id, status, date, amount, customer_region.

Добавить базовое логирование — логируй ключевые шаги или ошибки.

(Опционально) Реализовать проверку на дубли по order_id, чтобы не загружать повторно одни и те же заказы.


Ожидаемые файлы от тебя:

Python-скрипт(ы) — .py файлы

Структура БД (или сам .db файл, если SQLite)

README.md с описанием:

как запускать скрипт;

как устроен процесс;

какие шаги логируются;

как реализована проверка дубликатов (если есть)


Оценочное время выполнения:

2–4 часа

